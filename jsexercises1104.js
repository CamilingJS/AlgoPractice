// write fx:

// Adds two numbers

// divides two numbers

//multiplies 2 numbers

// These functions should only be called by a wrapper fx called "calculator"

//calculator should take a string like "2 times 3 power of 6 divded by 4"

//it should handle math left to right no Pemdas

//it should record all fxs in an array of numbers

// example:

//input
    //"2 times 4 divided 4 times 5 power 2 divided 2 "


//output
    // [8, 2, 10, 100, 50]



//     const add = (a, b) => (a + b);
    
//     const multiply = (a, b) => (a*b);
    
//     const division = (a, b) => (a/b);
    
//     const times = (a, b) => (a*b);
    
//     const power = (a, b) => (a**b);


//     let text = '2 times 4 divided 4 times 5 power 2 divided 2';
//     let splittedString = text.split(" ");
//     let calculator = (text) => {
//         let splittedString = text.split(" ");
//         splittedString.foreach( (parts, index) => {
//             switch(parts) {
//                 case "times": 
//                     multiply(splitstring[index-1], splitstring[index+1])
//                 break;
//                 case "divided":
//                     division(splitstring[index-1], splitstring[index+1])
//                 break;
//                 case "power": 
//                     power(splitstring[index-1], splitstring[index+1])
//                 break;
//                 case "plus":
//                     add(splitstring[index-1], splitstring[index+1])
//                 break;
//             }

//             return (parts);
//         } )
//     }

// console.log(calculator());


    


    

 


